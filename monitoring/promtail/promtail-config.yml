server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/positions.yaml

clients:
  - url: https://ca-loki.internal.ambitiousdune-51dd5b07.koreacentral.azurecontainerapps.io/loki/api/v1/push

scrape_configs:
  - job_name: scheduling-backend
    static_configs:
      - targets:
          - localhost
        labels:
          job: scheduling-backend
          __path__: /var/log/app/*.log
